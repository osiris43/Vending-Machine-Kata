<table>
  <tr>
    <th>Description</th>
    <th>Price</th>
  </tr>
  <% @items.each do |item| %>
    <tr>
      <td><%= item.location %></td>
      <td><%= item.description %></td>
      <td><%= number_to_currency(item.price) %></td>
    </tr>
  <% end %>
</table>

